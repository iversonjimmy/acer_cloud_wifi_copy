external_static_cpp_lib(
    name = 'libexif',
    # Note that includes are relative to $(PBROOT) for external_* makegen targets:
    includes = [
            'usr/include',
            'gvm_core/external/libexif/libexif'
            ],
    outfile = 'usr/lib/libexif.a'
)
