#include "Test.hpp"

class TestComponentSize : public Test {
public:
    TestComponentSize(u32 options);
    ~TestComponentSize();
    void RunTests();
    void PrintTestResult();
private:
    static const std::string dbpath;
    bool testPassed;
};
