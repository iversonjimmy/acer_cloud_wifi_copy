#include "Test.hpp"

class TestComponentPath : public Test {
public:
    TestComponentPath(u32 options);
    ~TestComponentPath();
    void RunTests();
    void PrintTestResult();
private:
    static const std::string dbpath;
    bool testPassed;
};
