#ifndef __PIPE_WIN_HPP__
#define __PIPE_WIN_HPP__

int read(int fd, char *buf, int len);
int pipe(int fildes[2]);
int write(int fd, char *buf, int len);
void close(int fd);

#endif /* __UNIXE_PIPE_HPP__ */
