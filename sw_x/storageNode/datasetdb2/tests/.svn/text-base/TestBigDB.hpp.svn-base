#include "Test.hpp"

class TestBigDB : public Test {
public:
    TestBigDB(u32 options);
    ~TestBigDB();
    void RunTests();
    void PrintTestResult();
private:
    static const std::string dbpath;
    bool testPassed;
};
