#include "Test.hpp"

class TestComponentMTime : public Test {
public:
    TestComponentMTime(u32 options);
    ~TestComponentMTime();
    void RunTests();
    void PrintTestResult();
private:
    static const std::string dbpath;
    bool testPassed;
};
