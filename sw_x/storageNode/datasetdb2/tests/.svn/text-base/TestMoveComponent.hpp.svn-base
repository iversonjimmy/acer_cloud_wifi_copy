#include "Test.hpp"

class TestMoveComponent : public Test {
public:
    TestMoveComponent(u32 options);
    ~TestMoveComponent();
    void RunTests();
    void PrintTestResult();
private:
    static const std::string dbpath;
    bool testPassed;
};
