#include "Test.hpp"

class TestDBConstraint : public Test {
public:
    TestDBConstraint(u32 options);
    ~TestDBConstraint();
    void RunTests();
    void PrintTestResult();
private:
    static const std::string dbpath;
    bool testPassed;
};
