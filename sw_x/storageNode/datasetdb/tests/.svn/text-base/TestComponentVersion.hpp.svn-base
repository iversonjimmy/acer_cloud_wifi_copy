#include "Test.hpp"

class TestComponentVersion : public Test {
public:
    TestComponentVersion(u32 options);
    ~TestComponentVersion();
    void RunTests();
    void PrintTestResult();
private:
    static const std::string dbpath;
    bool testPassed;
};
