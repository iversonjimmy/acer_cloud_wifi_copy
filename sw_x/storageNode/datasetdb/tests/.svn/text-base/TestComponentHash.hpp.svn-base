#include "Test.hpp"

class TestComponentHash : public Test {
public:
    TestComponentHash(u32 options);
    ~TestComponentHash();
    void RunTests();
    void PrintTestResult();
private:
    static const std::string dbpath;
    bool testPassed;
};
